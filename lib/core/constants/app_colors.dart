import 'package:flutter/material.dart';

/// Primary: Deep Indigo (trust, finance)
/// Accent:  Emerald (success, money)
/// Neutral: Warm Gray (comfortable reading)

class AppColors {
  AppColors._();

  static const Color primaryColor = Color(0XFFFF7A1A);
  static const Color titleColor = Color(0XFF1D1D35);
  static const Color textColor = Color(0XFF6E6E7E);
}